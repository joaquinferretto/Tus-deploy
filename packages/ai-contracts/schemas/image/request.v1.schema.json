{
  "$id": "https://golden-boilerplate.dev/ai-contracts/image/request.v1.schema.json",
  "title": "Image generation and editing request v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["contractVersion", "tenantId", "actorId", "correlationId", "lineage", "retention", "operation", "prompt", "mimeType", "model"],
  "properties": {
    "contractVersion": { "const": "1.0.0" },
    "tenantId": { "type": "string", "minLength": 1 },
    "actorId": { "type": "string", "minLength": 1 },
    "correlationId": { "type": "string", "minLength": 1 },
    "lineage": { "$ref": "#/$defs/lineage" },
    "retention": { "$ref": "#/$defs/retention" },
    "operation": { "enum": ["generate", "edit"] },
    "prompt": { "type": "string", "minLength": 1, "maxLength": 2000 },
    "sourceImageBase64": { "type": "string", "minLength": 1 },
    "sourceAssetId": { "type": ["string", "null"], "minLength": 1 },
    "sourceUri": { "type": ["string", "null"], "pattern": "^b2://" },
    "sourceChecksum": { "type": ["string", "null"], "pattern": "^[a-f0-9]{64}$" },
    "mimeType": { "enum": ["image/jpeg", "image/png", "image/webp"] },
    "fileName": { "type": ["string", "null"], "minLength": 1 },
    "model": { "type": "string", "minLength": 1 }
  },
  "$defs": {
    "lineage": {
      "type": "object",
      "additionalProperties": false,
      "required": ["rootMessageId", "source"],
      "properties": {
        "rootMessageId": { "type": "string", "minLength": 1 },
        "source": { "type": "string", "minLength": 1 },
        "sourceAssetId": { "type": ["string", "null"] }
      }
    },
    "retention": {
      "type": "object",
      "additionalProperties": false,
      "required": ["expiresAt", "encrypted", "keyRef", "algorithm"],
      "properties": {
        "expiresAt": { "type": "integer", "minimum": 0 },
        "encrypted": { "const": true },
        "keyRef": { "type": "string", "minLength": 1 },
        "algorithm": { "type": "string", "minLength": 1 }
      }
    }
  }
}
