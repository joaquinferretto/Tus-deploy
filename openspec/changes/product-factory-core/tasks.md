# Tasks: Product Factory Core

## Review Workload Forecast

| Field | Value |
|---|---|
| Estimated changed lines | 18,000–35,000 authored lines plus migrations, Terraform, fixtures, and docs |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR 1 is the next autonomous P0.6a native-local slice; later stacked PRs follow the existing P0/P1–P6 roadmap |
| Delivery strategy | force-chained |
| Chain strategy | stacked-to-main |

Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
400-line budget risk: High

### Suggested Work Units

| Unit | Goal | Likely PR | Focused test command | Runtime harness | Rollback boundary |
|---|---|---|---|---|---|
| 1 | P0.6a native local profile | PR 1 | `pnpm test -- foundation` | `cd backend && pnpm run dev`, then `cd frontend && pnpm run dev`, with PostgreSQL from root `.env`; Docker and live providers are out of scope | `backend/`, `frontend/`, native loader/config/docs/tests only |

**Next autonomous slice only:** P0.6a. P0.6b remains an independently open Docker gate; later Compose/reference slices retain it as a dependency.

**TDD contract:** every production task below starts with its named RED test, then GREEN implementation, then refactor/boundary checks; run `pnpm test` for every slice and `python -m pytest` for Python slices. Live AWS/provider smoke is activation-gated; deterministic fakes are mandatory.

## Phase P0: Deterministic Foundation and Governance

- [x] P0.1 | Deps — | Paths: `package.json`, `pnpm-workspace.yaml`, `turbo.json`, `pnpm-lock.yaml`, `packages/*/package.json` | RED root namespace/lockfile/lifecycle tests; GREEN normalize Node 20/pnpm 9 namespaces and scripts; evidence: `pnpm test` passes 14/14; rollback root manifests/lockfile; touches code/config/CI.
- [x] P0.2 | Deps P0.1 | Paths: `.gitignore`, `.env.example`, `.husky/*`, `scripts/security/*`, `.github/workflows/*` | RED tracked-secret and `.env`-disclosure tests; GREEN strong ignores, fictitious examples, pre-commit/CI blocking and rotation procedure; evidence: focused `p0-security` tests and `pnpm security:scan` pass; full slice remains gated by P0.6a native-local and P0.6b Docker runtime evidence; rollback security hooks/workflows only; config/docs/CI.
- [x] P0.3 | Deps P0.1 | Paths: `packages/contracts/schemas/**`, `packages/contracts/src/**`, `packages/contracts/generated/{typescript,python}/**` | RED TS/JSON/Python compatibility and unsupported-version tests; GREEN canonical schemas, checked-in bindings, AJV/jsonschema ingress-egress validation; evidence: focused contract tests and `pnpm contracts:validate` pass; full slice remains gated by P0.6a native-local and P0.6b Docker runtime evidence; rollback contract version; code/config.
- [x] P0.4 | Deps P0.3 | Paths: `packages/config/src/**`, `packages/errors/src/**`, `packages/lifecycle/src/**` | RED fail-fast production-config, redacted-error, health/readiness/shutdown tests; GREEN explicit profile/provider/security/quota/telemetry config and lifecycle contracts; evidence: focused config/lifecycle tests pass; full slice remains gated by P0.6a native-local and P0.6b Docker runtime evidence; rollback package slice; code/config.
- [x] P0.5 | Deps P0.3–P0.4 | Paths: `packages/observability/src/**`, `apps/api/src/middleware/**`, `apps/workflow-runtime-python/src/worker/telemetry/**` | RED correlation/tenant redaction and forbidden vendor-import tests; GREEN OTel-compatible context, structured logs, metrics and error capture through ports; evidence: Node observability 2/2, full `pnpm test` 14/14, and venv `pytest` 2/2; rollback telemetry middleware; code.
- [x] P0.6a.1 | Deps P0.1–P0.5 | Paths: `tests/foundation/native-profile/**` | RED spawned-process tests for exact wrapper commands, explicit root `.env` resolution, all four process/file `DATABASE_URL` precedence cases, missing-key non-readiness, redaction/no-copy, and process exit propagation; no Docker dependency.
- [x] P0.6a.2 | Deps P0.6a.1 | Paths: `backend/package.json`, `frontend/package.json`, `scripts/dev/native-profile.mjs` | GREEN source-free wrappers whose exact `dev` scripts delegate to `apps/api`/`apps/web`; loader consumes only root `.env` `DATABASE_URL`, gives non-empty process env precedence, and never logs values or copies `.env`.
- [x] P0.6a.3 | Deps P0.6a.2 | Paths: `tests/foundation/native-profile/**` | RED native readiness tests for required PostgreSQL plus individually modeled MongoDB, Redis, Python worker, mobile support, and external-provider optional/disabled/fake/unavailable states and no cloud calls.
- [x] P0.6a.4 | Deps P0.6a.3 | Paths: `packages/config/src/**`, `apps/api/src/{index.ts,server.ts,presentation/routes/health.ts}`, `apps/web/**` | GREEN native bootstrap requires PostgreSQL and emits the six redacted dependency reports without weakening readiness or Compose requirements.
- [x] P0.6a.5 | Deps P0.6a.4 | Paths: `docs/runbooks/local-profiles.md`, `docs/evidence/native-smoke.md`, `tests/foundation/native-profile/**` | REFACTOR and boundary checks prove source-free/no-secret-copy, native-versus-Compose evidence separation, and a deterministic provider-free rollback scenario stops partial serving while ledger/outbox/DLQ work remains replayable and version/reason/health evidence is recorded.
- [x] P0.6a.6 | Deps P0.6a.5 | Paths: `docs/evidence/native-smoke.md` | Run real local commands `cd backend && pnpm run dev` and `cd frontend && pnpm run dev`, record API/web health, PostgreSQL connectivity, six dependency states, scans, exact `pnpm test -- foundation` and `pnpm test` results; record deterministic rollback verification without Docker or cloud calls.
- [ ] P0.6b | Deps P0.1–P0.5 | Paths: `docker-compose.yml`, `Makefile`, `scripts/dev/**`, `docs/development/**` | RED clean-checkout/provider-free readiness tests; GREEN complete Compose API/web/mobile support/Python/Postgres/Mongo/Redis/fakes harness; evidence: `pnpm test`; runtime: `docker compose config` and full Compose boot/readiness. This remains a separate unchecked Docker integration gate because Docker is unavailable.
- [ ] P0.7 | Deps P0.3–P0.6a/P0.6b | Paths: `docs/architecture/capability-ledger.md`, `docs/architecture/ownership-matrix.md`, `docs/architecture/contamination-map.md`, `docs/architecture/profile-matrix.md` | RED traceability completeness test; GREEN version owner/dependency/contract/path/fake/use-case/security-data-cost/evidence rows and neutral boundaries; evidence: focused governance tests pass only after both profile gates; rollback governance docs; docs.
- [ ] P0.8 | Deps P0.3–P0.7 | Paths: `infra/terraform/modules/{network,compute,data,cache,storage,edge,queue,observability,security,backup,governance}/**`, `infra/terraform/environments/**` | RED Terraform module/profile fixture tests; GREEN explicit capability modules, flags, region/tags/encryption/retention/quota inputs with no CDK; evidence: Terraform v1.15.8 `fmt -check -recursive` and `validate` after safe `init -backend=false`; no resources provisioned; rollback module/environment directory; IaC/config.

## Phase P1: Identity, Tenancy, and Privacy

- [ ] P1.1 | Deps P0.3,P0.7 | Paths: read-only `C:\Users\mmmau\auth-kit-standalone`, read-only `C:\Users\mmmau\vialovers-worktrees\alqui-full-product\apps\server\auth-kit`, `docs/security/auth-provenance.md` | RED provenance/read-only integrity and regression cases; GREEN record reusable evidence without copying or modifying either source; evidence: `pnpm test`; rollback provenance docs; docs/tests only.
- [ ] P1.2 | Deps P1.1 | Paths: `apps/api/src/auth-security/{domain,application,ports,adapters,http}/**`, `packages/contracts/schemas/identity/**` | RED verified sign-in, non-enumerating failure and privilege-escalation tests; GREEN neutral password/verification/recovery/account/credential lifecycle; evidence: `pnpm test`; rollback auth successor modules; code.
- [ ] P1.3 | Deps P1.2 | Paths: `apps/api/prisma/schema.prisma`, `apps/api/prisma/migrations/**`, `apps/api/prisma/seed.ts`, `apps/api/src/auth-security/adapters/postgres/**` | RED schema/seed tenant-safe identity tests; GREEN Prisma models/migrations/seeds for users, accounts, credentials, verification and recovery; evidence: `pnpm test`; rollback migration version/seed; code/config.
- [ ] P1.4 | Deps P1.2–P1.3 | Paths: `apps/api/src/auth-security/application/refresh/**`, `apps/api/src/auth-security/adapters/postgres/sql/**` | RED concurrent refresh replay/retry tests; GREEN parameterized atomic family lock/hash rotation, revocation, device sessions, restart recovery and outbox event; evidence: `pnpm test`; rollback refresh migration/adapter; code.
- [ ] P1.5 | Deps P1.2 | Paths: `apps/api/src/auth-security/{mfa,passkeys,oauth-oidc,account-linking}/**`, `packages/contracts/schemas/auth/**` | RED MFA/passkey/OAuth/OIDC/linking denial and callback tests; GREEN provider-neutral flows with explicit issuer/client/credential gates; evidence: `pnpm test`; runtime: local fake IdP; rollback auth feature flags/modules; code/config.
- [ ] P1.6 | Deps P1.3–P1.5 | Paths: `apps/api/src/tenancy/{domain,application,ports,adapters,http}/**`, `packages/contracts/schemas/tenancy/**` | RED cross-tenant/direct-search and deny-by-default tests; GREEN organizations/workspaces/memberships/roles/permissions/RBAC-ABAC tenant context; evidence: `pnpm test`; rollback tenancy modules/migrations; code.
- [ ] P1.7 | Deps P1.6 | Paths: `apps/api/src/admin/product-superadmin/**`, `apps/api/src/audit/**` | RED product-boundary escalation and audited policy rollback tests; GREEN separate per-product superadmin, immutable/redacted security/admin events and correlation metadata; evidence: `pnpm test`; rollback admin/audit slice; code.
- [ ] P1.8 | Deps P1.6–P1.7 | Paths: `apps/api/src/privacy/**`, `packages/contracts/schemas/privacy/**` | RED unauthorized export/deletion, consent withdrawal and retention tests; GREEN consent/opt-out/export/deletion propagation, redaction, configurable retention and audit; evidence: `pnpm test`; rollback privacy job version; code/docs.

## Phase P2: Data, CRUD, and Consistency Ledger

- [ ] P2.1 | Deps P0.7,P1.3 | Paths: `apps/api/prisma/schema.prisma`, `apps/api/prisma/migrations/**`, `apps/api/prisma/seed.ts`, `docs/data/ownership.md` | RED owner/rebuild/2PC prohibition tests; GREEN PostgreSQL source-of-truth models for auth, tenancy, audit, search, aggregates, quotas, ledger/outbox; evidence: `pnpm test`; rollback migration; code/docs.
- [ ] P2.2 | Deps P2.1 | Paths: `apps/api/src/data/mongo/{ports,adapters,sessions,reconciliation}/**`, `apps/api/src/data/ownership/**` | RED Mongo tenant filter/session failure tests; GREEN explicit document/read-model ownership, Mongo ACID sessions and rebuild/reconciliation status; evidence: `pnpm test`; rollback Mongo projection; code.
- [ ] P2.3 | Deps P2.1–P2.2 | Paths: `apps/api/src/platform/crud/**`, `packages/contracts/schemas/crud/**` | RED tenant-safe pagination/filter/search tests; GREEN generic CRUD/search ports and Prisma repositories with no vendor imports in domain/application; evidence: `pnpm test`; rollback CRUD routes/modules; code.
- [ ] P2.4 | Deps P2.1–P2.3 | Paths: `apps/api/src/data/postgres/sql/{locks,aggregates,bulk,full-text,geospatial,hot-paths}.ts` | RED concurrent invariant and injection tests; GREEN parameterized raw SQL for locks, CTE/window/aggregate/bulk/full-text/geospatial/hot paths; evidence: `pnpm test`; rollback SQL repository version; code.
- [ ] P2.5 | Deps P2.2–P2.4 | Paths: `apps/api/src/platform/assets/**`, `packages/contracts/schemas/assets/**`, `packages/storage/src/{ports,b2,s3-staging,fakes}/**` | RED asset ownership/lineage/delete and transient-S3 tests; GREEN B2 durable source, encrypted lifecycle-managed S3 staging, metadata, access policy and cleanup; evidence: `pnpm test`; runtime: B2/S3 fakes; rollback asset adapter; code/config.
- [ ] P2.6 | Deps P2.1,P2.3 | Paths: `apps/api/src/platform/notifications/**`, `packages/contracts/schemas/notifications/**`, `packages/email/src/**` | RED preference/quota/retry/provider-outage tests; GREEN notifications, email status/templates/preferences, deterministic fake and SES-gated port; evidence: `pnpm test`; rollback notification adapter; code/config.
- [ ] P2.7 | Deps P2.1,P1.6 | Paths: `apps/api/src/platform/{flags,configuration}/**`, `packages/config/src/**` | RED profile/tenant/product flag resolution and fail-fast tests; GREEN explicit feature flags/config scopes and reversible resolution; evidence: `pnpm test`; rollback flag schema/version; code/config.
- [ ] P2.8 | Deps P2.1,P1.6 | Paths: `apps/api/src/platform/quotas/**`, `packages/contracts/schemas/quotas/**` | RED deterministic exhaustion/cross-tenant accounting tests; GREEN reservation, usage/cost units, rate limits, budgets and quota enforcement; evidence: `pnpm test`; rollback quota policy version; code.
- [ ] P2.9 | Deps P2.1,P2.4 | Paths: `apps/api/src/platform/idempotency/**`, `apps/api/src/platform/outbox/**` | RED duplicate/replay/claim race tests; GREEN atomic parameterized idempotency and transactional outbox records/publication/claim/recovery; evidence: `pnpm test`; rollback SQL/migration slice; code.
- [ ] P2.10 | Deps P2.1,P2.9 | Paths: `apps/api/src/platform/{jobs,events,run-ledger,sagas,reconciliation}/**`, `packages/contracts/schemas/jobs/**` | RED crash-before-ack, compensation and reconciliation tests; GREEN durable run ledger, saga states, producer/consumer contracts and recoverable failure states; evidence: `pnpm test`; rollback ledger worker version; code.
- [ ] P2.11 | Deps P2.1–P2.10 | Paths: `packages/contracts/traceability/**`, `docs/architecture/capability-ledger.md` | RED missing-owner/missing-evidence checks for every P2 ledger row; GREEN separate rows for users, sessions, verification, tenants, roles, audit, CRUD, assets, notifications, flags, quotas, idempotency/outbox, jobs/events, privacy; evidence: `pnpm test`; rollback ledger docs/check; docs/tests.
- [ ] P2.L1 | Deps P1.2–P1.3 | Paths: `apps/api/src/auth-security/users/**`, `apps/api/prisma/schema.prisma` | RED tenant-safe users/accounts/credentials CRUD; GREEN hash/lifecycle/audit implementation and ledger row; evidence: `pnpm test`; rollback user models/module; code.
- [ ] P2.L2 | Deps P1.4 | Paths: `apps/api/src/auth-security/sessions/**`, `apps/api/prisma/migrations/**` | RED atomic rotation/revocation/replay/restart tests; GREEN sessions/refresh/devices ledger row; evidence: `pnpm test`; rollback session migration/module; code.
- [ ] P2.L3 | Deps P1.2 | Paths: `apps/api/src/auth-security/verification-recovery/**`, `packages/email/src/**` | RED expiry/replay/delivery-failure/non-enumeration tests; GREEN verification/recovery ledger row with fake email evidence; evidence: `pnpm test`; rollback token/email module; code.
- [ ] P2.L4 | Deps P1.6 | Paths: `apps/api/src/tenancy/organizations/**`, `apps/api/src/tenancy/memberships/**` | RED default-organization/isolation tests; GREEN organizations/workspaces/memberships/tenants ledger row; evidence: `pnpm test`; rollback tenancy migration/module; code.
- [ ] P2.L5 | Deps P1.6–P1.7 | Paths: `apps/api/src/tenancy/policy/**`, `apps/api/src/admin/product-superadmin/**` | RED deny-by-default/escalation/admin-audit tests; GREEN roles/permissions/tenant authorization/superadmin ledger row; evidence: `pnpm test`; rollback policy version; code.
- [ ] P2.L6 | Deps P1.7 | Paths: `apps/api/src/audit/**`, `apps/api/prisma/schema.prisma` | RED redaction/correlation/retention/query tests; GREEN audit/security-events ledger row; evidence: `pnpm test`; rollback audit migration/module; code.
- [ ] P2.L7 | Deps P2.3 | Paths: `apps/api/src/platform/crud/**`, `apps/api/src/platform/search/**` | RED pagination/filter/tenant-query tests; GREEN generic CRUD/search ledger row and neutral reference contract; evidence: `pnpm test`; rollback CRUD/search module; code.
- [ ] P2.L8 | Deps P2.5 | Paths: `apps/api/src/platform/assets/**`, `packages/storage/src/**` | RED ownership/lineage/lifecycle/delete tests; GREEN assets/uploads/lineage ledger row; evidence: `pnpm test`; rollback asset module; code.
- [ ] P2.L9 | Deps P2.6 | Paths: `apps/api/src/platform/notifications/**`, `packages/email/src/**` | RED delivery status/retry/preferences tests; GREEN notifications/email ledger row with SES gate and fake; evidence: `pnpm test`; rollback notification module; code.
- [ ] P2.L10 | Deps P2.7 | Paths: `apps/api/src/platform/{flags,configuration}/**`, `packages/config/src/**` | RED profile/tenant rollback tests; GREEN feature-flags/configuration ledger row; evidence: `pnpm test`; rollback flag/config version; code/config.
- [ ] P2.L11 | Deps P2.8 | Paths: `apps/api/src/platform/quotas/**`, `apps/api/src/platform/rate-limits/**` | RED reservation/exhaustion/cross-tenant tests; GREEN quotas/rate-limits ledger row with cost units; evidence: `pnpm test`; rollback quota policy; code.
- [ ] P2.L12 | Deps P2.4,P2.9 | Paths: `apps/api/src/platform/{idempotency,outbox}/**` | RED duplicate/claim/publication/recovery races; GREEN idempotency/outbox ledger row with atomic SQL; evidence: `pnpm test`; rollback migration/SQL slice; code.
- [ ] P2.L13 | Deps P2.10,P3.4 | Paths: `apps/api/src/platform/{jobs,events,run-ledger}/**`, `packages/queues/src/**` | RED crash/replay/DLQ/reconciliation tests; GREEN jobs/events/run-ledger ledger row; evidence: `pnpm test` + `python -m pytest`; rollback worker/transport version; code.
- [ ] P2.L14 | Deps P1.8,P2.10 | Paths: `apps/api/src/privacy/**`, `apps/api/src/recovery/**` | RED consent/deletion/export propagation tests; GREEN privacy/retention/deletion ledger row with tracked completion; evidence: `pnpm test`; rollback privacy job version; code/docs.

## Phase P3: Durable LangGraph Runtime and RAG

- [ ] P3.1 | Deps P0.3,P2.9–P2.10 | Paths: `apps/workflow-runtime-python/src/worker/contracts/**`, `packages/contracts/schemas/workflows/**`, `tests/compatibility/**` | RED TS/JSON/Python message drift tests; GREEN versioned tenant/actor/correlation/idempotency/lineage messages and compatibility fixtures; evidence: `pnpm test` + `python -m pytest`; rollback contract version; code/tests.
- [ ] P3.2 | Deps P3.1 | Paths: `apps/workflow-runtime-python/src/worker/langgraph/{registry,graphs,subgraphs,supervisors}/**` | RED competing-authority and Agents/Flows exclusion tests; GREEN LangGraph-only registry owning graph/state/session/tool/RAG/evaluation orchestration; evidence: `pnpm test` + `python -m pytest`; rollback registry release; code.
- [ ] P3.3 | Deps P2.10,P3.2 | Paths: `apps/workflow-runtime-python/src/worker/{checkpoint,memory,hitl,policy,run-ledger}/**` | RED pause/resume, unauthorized tool/HITL and crash/replay tests; GREEN durable checkpoints, threads, approvals, deny-by-default policy and ledger idempotency; evidence: `python -m pytest` + `pnpm test`; rollback worker version; code.
- [ ] P3.4 | Deps P2.10,P3.3 | Paths: `packages/queues/src/{ports,redis-local,sqs-dlq,fakes}/**`, `apps/workflow-runtime-python/src/worker/delivery/**` | RED claim/ack/retry/backoff/poison/cancel/reconcile tests; GREEN equivalent Redis-local and SQS+DLQ transports with no queue-owned state; evidence: `pnpm test` + `python -m pytest`; AWS activation-gated; rollback transport adapter; code/config.
- [ ] P3.5 | Deps P3.4 | Paths: `packages/events/src/{eventbridge,lambda,fakes}/**`, `apps/workflow-runtime-python/src/worker/lifecycle/**` | RED adapter retry/redaction and worker shutdown tests; GREEN EventBridge/Lambda adapters where capability use exists and graceful worker lifecycle; evidence: `pnpm test` + `python -m pytest`; AWS activation-gated; rollback event adapter; code/config.
- [ ] P3.6 | Deps P3.3–P3.5 | Paths: `packages/contracts/schemas/streams/**`, `apps/workflow-runtime-python/src/worker/streaming/**`, `apps/api/src/streams/**` | RED reconnect cursor/order/backpressure/cancel tests; GREEN correlated token/audio/event/progress streams and partial-result policy; evidence: `pnpm test` + `python -m pytest`; rollback stream protocol version; code.
- [ ] P3.7 | Deps P2.5,P3.2 | Paths: `apps/workflow-runtime-python/src/worker/rag/{ingest,parsing,chunking,dedup,lineage}/**`, `packages/contracts/schemas/rag/**` | RED unsafe/oversized/duplicate/partial-ingest tests; GREEN B2-authorized parsing, safe chunking, deduplication, checksum, lineage, retention and quarantine; evidence: `python -m pytest` + `pnpm test`; rollback parser/index version; code.
- [ ] P3.8 | Deps P3.7,P2.1 | Paths: `apps/workflow-runtime-python/src/worker/rag/{embeddings,pgvector}/**`, `apps/api/prisma/schema.prisma`, migrations | RED embedding/index tenant-filter tests; GREEN AWS embedding port, PGVector ownership/versioning and B2-rebuildable metadata; evidence: `python -m pytest` + `pnpm test`; AWS activation-gated; rollback index version/migration; code/config.
- [ ] P3.9 | Deps P3.8 | Paths: `apps/workflow-runtime-python/src/worker/rag/{retrieval,reranking,citations,graph}/**` | RED tenant leakage/uncited answer/provider timeout tests; GREEN LangGraph lexical/vector fusion, rank fusion/reranking, limits, citations and explicit partial/unavailable status; evidence: `python -m pytest`; rollback graph/index selector; code.
- [ ] P3.10 | Deps P3.7–P3.9,P1.8 | Paths: `apps/workflow-runtime-python/src/worker/rag/{update,delete,reindex,reconciliation,evaluations}/**`, `docs/rag/**` | RED deletion propagation and degraded-reindex rollback tests; GREEN update/delete/reindex/stale detection, recall/isolation/citation/quality/latency/cost fixtures and retained last-passing index; evidence: `python -m pytest` + `pnpm test`; rollback selector/worker; code/docs.

## Phase P4: Complete AI Capability Catalog

- [ ] P4.1 | Deps P3.2–P3.3 | Paths: `packages/ai-contracts/schemas/{llm,chat,structured-output,tools}/**`, `apps/workflow-runtime-python/src/worker/ai/{llm,structured,tools}/**` | RED schema/tool-denial/usage tests; GREEN Groq-active/fake LLM chat, structured output and typed permissioned tools; evidence: `python -m pytest` + `pnpm test`; rollback provider routing; code.
- [ ] P4.2 | Deps P3.2–P3.3 | Paths: `apps/workflow-runtime-python/src/worker/ai/{agents,subgraphs,supervisors,memory,sessions}/**` | RED tenant-isolation/resume/supervisor tests; GREEN LangGraph agents/subgraphs/supervisors, memory and session/checkpoint catalog rows; evidence: `python -m pytest`; rollback graph registry version; code.
- [ ] P4.3 | Deps P2.1,P3.2 | Paths: `apps/api/prisma/schema.prisma`, `apps/api/src/ai/registry/**`, `apps/workflow-runtime-python/src/worker/ai/registry/**` | RED approval/rollout/deprecation/provider-failure/rollback tests; GREEN PromptDefinition, PromptVersion, ModelDefinition, ModelAvailability, Rollout, Approval, RegistryAudit and fake evaluator; evidence: `pnpm test` + `python -m pytest`; rollback registry migration/version; code.
- [ ] P4.4 | Deps P4.1–P4.3 | Paths: `apps/workflow-runtime-python/src/worker/ai/{routing,retry,circuit-breaker,usage,cost,quota}/**` | RED timeout/circuit-open/silent-switch and cost-quota tests; GREEN explicit routing, bounded retry/fallback metadata, circuit breaking, usage/cost accounting and hard tenant quotas; evidence: `python -m pytest` + `pnpm test`; rollback routing policy; code.
- [ ] P4.5 | Deps P1.8,P4.4 | Paths: `apps/workflow-runtime-python/src/worker/ai/{guardrails,privacy,retention}/**`, `packages/contracts/schemas/ai-governance/**` | RED unsafe-content/HITL/redaction/consent-withdrawal tests; GREEN guardrails, privacy, encryption/lineage/opt-out/deletion/retention audit; evidence: `python -m pytest` + `pnpm test`; rollback policy version; code/config.
- [ ] P4.6 | Deps P4.1–P4.5 | Paths: `apps/workflow-runtime-python/src/worker/ai/{evaluations,regression}/**`, `docs/ai/catalog-ledger.md` | RED missing-contract/fake/owner/eval row checks; GREEN independent evidence rows for LLM/chat, structured output, tools, agents, memory, registry, routing, cost, guardrails, streaming and RAG; evidence: `python -m pytest` + `pnpm test`; rollback eval catalog; docs/tests.
- [ ] P4.7 | Deps P4.1,P3.4 | Paths: `packages/ai-contracts/schemas/{stt,tts}/**`, `apps/workflow-runtime-python/src/worker/ai/{stt,tts}/**`, `packages/providers/src/groq/**` | RED sanitized audio/error/quota tests; GREEN Groq-active/fake STT/TTS and gated provider ports with lineage/retention; evidence: `python -m pytest` + `pnpm test`; paid/live activation-gated; rollback speech adapters; code/config.
- [ ] P4.8 | Deps P4.1 | Paths: `packages/ai-contracts/schemas/translation/**`, `apps/workflow-runtime-python/src/worker/ai/translation/**` | RED language/tenant/safety tests; GREEN typed translation implementation/fake, quotas, redacted telemetry and availability disposition; evidence: `python -m pytest`; Bedrock activation-gated; rollback translation adapter; code/config.
- [ ] P4.9 | Deps P4.1 | Paths: `packages/ai-contracts/schemas/vision/**`, `apps/workflow-runtime-python/src/worker/ai/vision/**` | RED media-size/tenant/redaction tests; GREEN multimodal vision implementation/fake, lineage, safety, quota and provider disposition; evidence: `python -m pytest`; activation-gated; rollback vision adapter; code/config.
- [ ] P4.10 | Deps P4.1,P3.7 | Paths: `packages/ai-contracts/schemas/ocr/**`, `apps/workflow-runtime-python/src/worker/ai/ocr/**` | RED unsafe/oversized document tests; GREEN OCR/document-intelligence implementation/fake with B2 lineage, retention and cost evidence; evidence: `python -m pytest`; activation-gated; rollback OCR adapter; code/config.
- [ ] P4.11 | Deps P4.1,P4.5 | Paths: `packages/ai-contracts/schemas/moderation/**`, `apps/workflow-runtime-python/src/worker/ai/moderation/**` | RED unsafe fixture/deny-HITL/redaction tests; GREEN moderation/labels implementation/fake and audited safety outcome; evidence: `python -m pytest`; activation-gated; rollback moderation policy; code/config.
- [ ] P4.12 | Deps P2.8,P4.4 | Paths: `packages/ai-contracts/schemas/recommendations/**`, `apps/workflow-runtime-python/src/worker/ai/recommendations/**`, `docs/ai/recommendations.md` | RED relevance/safety/latency/cost/tenant tests; GREEN neutral recommendation contract, real implementation, fake, quota, owner, cross-project use case and AWS alternative/disposition; evidence: `python -m pytest` + `pnpm test`; rollback recommendation adapter; code/docs.
- [ ] P4.13 | Deps P2.5,P4.5 | Paths: `packages/ai-contracts/schemas/image/**`, `apps/workflow-runtime-python/src/worker/ai/image/**` | RED input-safety/lineage/retention tests; GREEN image generation/editing implementation/fake, staged asset lifecycle, cost/quota and availability disposition; evidence: `python -m pytest`; activation-gated; rollback image adapter; code/config.
- [ ] P4.14 | Deps P2.5,P2.8,P3.4,P4.5 | Paths: `packages/ai-contracts/schemas/video/**`, `apps/workflow-runtime-python/src/worker/ai/video/**` | RED async progress/cancel/cleanup/quota tests; GREEN video generation/editing contract with real async implementation, fake, progress, cancellation, lineage, retention, safety/cost evidence and alternative/disposition; evidence: `python -m pytest` + `pnpm test`; activation-gated; rollback video worker/version; code/docs.
- [ ] P4.15 | Deps P4.1–P4.14 | Paths: `apps/workflow-runtime-python/src/worker/ai/bedrock/**`, `packages/providers/src/{bedrock,guardrails,transcribe,polly,textract,rekognition,translate}/**`, `docs/ai/activation-gates.md` | RED no-credit/no-credential/no-region tests; GREEN complete wired fake-tested Bedrock/specialized adapters, Groq remains active, activation requires credits/credentials/region/quota/owner live conformance; evidence: `python -m pytest` + `pnpm test`; rollback flags only; code/config/docs.

## Phase P5: Providers and Neutral Reference Integration

- [ ] P5.1 | Deps P2.5,P3.4,P4.15 | Paths: `packages/providers/src/{b2,postgres,mongo,redis,sqs,aws,email,groq,notifications}/**`, `packages/providers/tests/**` | RED success/timeout/retry/duplicate/quota/redaction conformance tests; GREEN configured ports/adapters/fakes with vendor SDKs isolated; evidence: `pnpm test` + `python -m pytest`; rollback provider registry; code.
- [ ] P5.2 | Deps P2.9,P2.10 | Paths: `apps/api/src/providers/mercado-pago/**`, `packages/contracts/schemas/payments/**`, `apps/reference/fallback/payment/**` | RED forged/replayed webhook and saga/reconciliation tests; GREEN signed receipt-before-processing, idempotency/outbox, compensation, retry/DLQ and fake; runtime: local webhook fixture; rollback adapter/reference scenario; code.
- [ ] P5.3 | Deps P2.9,P2.10 | Paths: `apps/api/src/providers/whatsapp/**`, `packages/contracts/schemas/messaging/**`, `apps/reference/fallback/messaging/**` | RED signature/replay/tenant-policy tests; GREEN normalized secure webhook, outbox/saga/reconciliation, retry/DLQ and fake; runtime: local webhook fixture; rollback adapter/reference scenario; code.
- [ ] P5.4 | Deps P0.6b,P1–P5.3 | Paths: `apps/reference/api/**`, `apps/reference/web/**`, `apps/reference/mobile/**`, `apps/web/**`, `apps/mobile/**` | RED end-to-end neutral contract tests; GREEN signup/verification, membership, generic CRUD/search, assets, notifications, jobs, AI/RAG, audit, telemetry and superadmin state/intent clients; evidence: `pnpm test`; runtime: complete Compose reference flow; rollback reference wiring only; code/docs.
- [ ] P5.5 | Deps P0.6b,P5.4 | Paths: `apps/reference/fallback/{marketplace,messaging,settlement}/**`, `docs/reference/fallback-boundaries.md` | RED import/contamination tests; GREEN only isolated fallback fixtures required to prove universal capabilities, with no vertical names/schemas/prompts/routes in core; evidence: `pnpm test`; rollback fallback directory; code/docs.
- [ ] P5.6 | Deps P0.6b,P5.4–P5.5 | Paths: `scripts/validation/{contamination,reference-parity}.ts`, `docs/evidence/reference/**` | RED cross-tenant/failure/retry/rollback/contamination tests; GREEN evidence artifact and parity checks for API/web/mobile/runtime without client policy bypass; evidence: `pnpm test`; runtime: clean Compose smoke; rollback validation scripts/docs; code/docs.

## Phase P6: Hardening, Portability, Recovery, and Activation

- [ ] P6.1 | Deps P0.8,P5.1 | Paths: `infra/terraform/modules/**`, `infra/terraform/environments/aws/**` | RED module/profile drift/duplicate-store tests; GREEN Terraform AWS critical catalog: VPC, ECS/Fargate, ECR, RDS/Aurora, Mongo decision adapter, ElastiCache, S3, CloudFront, WAF, Route53/ACM, SQS/DLQ, EventBridge, Lambda, CloudWatch/OTel, IAM/STS/KMS, CloudTrail, GuardDuty, Security Hub, Inspector, Backup, Budgets, Service Quotas; evidence: `terraform validate/plan`; AWS activation-gated; rollback module/environment state.
- [ ] P6.2 | Deps P0.6a,P0.6b,P6.1 | Paths: `render.yaml`, `docs/deployment/render.md`, `docs/deployment/aws.md`, `infra/terraform/environments/render/**` | RED profile parity/no-production-Docker tests; GREEN Render-native API/web/Python workers with Neon, managed Mongo, Redis, B2 plus AWS profile shape; evidence: `pnpm test` + `terraform validate`; runtime: Render dry-run/AWS plan; rollback profile config; config/IaC/docs.
- [ ] P6.3 | Deps P0.2,P6.1–P6.2 | Paths: `.github/workflows/security.yml`, `.github/workflows/ci.yml`, `scripts/security/**`, `docs/security/incident-response.md` | RED staged-secret/credential-print/rotation tests; GREEN scan blocking, tracked-secret detection, minimum STS AssumeRole bootstrap and secret-store-only values; evidence: `pnpm test`; live credentials never used; rollback CI/security policy; config/docs.
- [ ] P6.4 | Deps P0.5,P6.1 | Paths: `packages/observability/src/**`, `infra/terraform/modules/{observability,security,governance}/**`, `docs/operations/telemetry.md` | RED missing trace/metric/audit/budget/quota evidence tests; GREEN OTel/CloudWatch, CloudTrail, security findings, budgets, quotas, alerts and redacted evidence; evidence: `pnpm test` + `terraform validate`; rollback observability/control modules; code/IaC/docs.
- [ ] P6.5 | Deps P1.8,P2.10,P3.10 | Paths: `apps/api/src/recovery/**`, `infra/terraform/modules/backup/**`, `docs/operations/{backup,recovery,retention}.md` | RED crash/restore/DLQ/reconciliation/deletion tests; GREEN verified backups, restore drills, ledger/DLQ replay, retention/deletion propagation and recovery evidence; evidence: `pnpm test` + `python -m pytest`; paid/live backup smoke activation-gated; rollback runbook/version.
- [ ] P6.6 | Deps P5.6,P6.1–P6.5 | Paths: `scripts/validation/portability/**`, `docs/operations/portability.md`, `docs/evidence/profile-parity.md` | RED fresh-checkout/profile-divergence tests; GREEN clean-environment Compose bootstrap, Render/AWS parity matrix, contamination scan and explicit unsupported-profile result; evidence: `pnpm test`; runtime: clean checkout without reading `.env`; rollback evidence scripts/docs.
- [ ] P6.7 | Deps all prior phases | Paths: `docs/runbooks/**`, `docs/evidence/readiness/**`, `docs/rollback/**` | RED missing/expired/live-unauthorized readiness tests; GREEN runbooks for provider disablement, migration rollback, job replay, backup restore, secret rotation, profile rollback and evidence-backed readiness; evidence: `pnpm test`; rollback docs only; docs.
- [ ] P6.8 | Deps P4.15,P5.1,P6.1–P6.7 | Paths: `scripts/activation/**`, `docs/activation-gates.md` | RED denied activation without credits/credentials/region/quota/owner approval; GREEN explicit gates for Bedrock and all paid/live AWS/SES/provider smokes, preserving local fakes and Groq active; evidence: `pnpm test`; live smoke only after explicit authorization; rollback flags/records.
- [ ] P6.9 | Deps all prior tasks | Paths: `docs/evidence/final-traceability.md`, `openspec/changes/product-factory-core/**` | RED incomplete capability/AI ledger and scenario coverage check; GREEN final traceability mapping every spec scenario and ledger row to implementation, fake, test, owner, config, evidence and rollback; evidence: `pnpm test`; rollback artifact only; docs.

## Threat-Matrix Coverage

The design marks documentation-like paths, Git repository selection, commit state, push state, and PR commands as `N/A`; no threat RED tasks are added. All applicable security, tenancy, webhook, secret, replay, provider, and boundary cases above have explicit RED tests before implementation tasks.

## Implementation Order / Guard

Apply the next autonomous slice **P0.6a.1–P0.6a.6** first; it depends only on P0.1–P0.5 and never on Docker. Keep P0.6b independently open, then apply P0.7/P0.8 and the remaining P0–P6 roadmap in dependency order; full Compose/reference tasks retain P0.6b. Each stacked PR merges toward `main` only after its RED, GREEN, refactor/boundary checks, real command evidence, and rollback boundary pass. No task bypasses activation gates, paid/live smoke approval, or the `pnpm test` TDD gate.
